import { css } from '@emotion/core';

const TitleStyles = css`
  text-align: center;
  color: #fff;
  position: relative;
  p {
    font-size: 17px;
  }
  h2 {
    margin-top: 10px;
    font-size: 40px;
    line-height: 1;
    font-weight: 600;
  }
`;

export default TitleStyles;
